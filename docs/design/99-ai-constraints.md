# 99. AI 生成內容時的總限制

## 99.1 限制清單

**這裡只放設計文件裡查不到的規則。** 所有能在設計文件讀到的規格（數值、公式、機制）
一律去該文件看，不在此重述 —— 重述必然與來源不同步。功能對應哪份文件見 `INDEX.md`。

1. 陣營只有艾爾薩斯王國與瓦爾登聯邦兩個，**不可新增第三陣營**（陣營規則見 `10-faction.md`）
2. **不可設計等級突破或覺醒系統**（等級無硬上限，見 `04-character.md` § 4.9）
3. 裝備模板一律**用 id 查表，不可用名字查**（禁用 `db.equipmentTemplates.where('name')`）——
   seed 換過 id 的品項會在玩家 IndexedDB 留下同名舊列，名字查表會撈到舊數值。
   孤兒模板由 `client/src/db/seed/purgeStaleTemplates.ts` 在每次 seed 後清除
4. 新手裝名單**只有 seed 一個來源**：`acquireType: 'starter'` + `requiredClass`
   （無 `requiredClass` = 全職業共用）。不可在程式碼另立硬編名單
5. 傳進戰鬥系統的 `equippedGear` 是**陣列，順序＝instance id 插入順序，不是部位順序**。
   取手持武器一律用 `systems/combat.ts` 的 `getEquippedWeapon()`，**禁用 `equippedGear[0]`** ——
   換掉新手武器後會靜默取到防具，武器基傷／額外攻擊／材質克制全部失效且不報錯
6. 技能的 `requiredWeaponType`（如三連射的【需裝備弓】）是**實際限制**，
   由 `scriptRunner.ts` 的 `meetsWeaponRequirement()` 在腳本選招時擋下，
   不可退化成只在 tooltip／Wiki 顯示的裝飾字
7. 背包／倉庫一律**用道具 id 查，不可用名字查**（禁用 `characterBag.where({ name })`、
   `bagItems.find(b => b.name === ...)`）—— 道具改名後舊名對不上，玩家存量等於消失。
   `BagItem.name` 與 `type` 是由 `itemId` 反查 seed 產生的顯示快取，一律經
   `models/bagItem.ts` 的 `makeBagItem()` 產生，不可手寫。
   同理，**設定表指涉道具或裝備一律存 id**（卷軸、狀態解除道具、印記、技能書、
   `craftMaterials`、`craftPrerequisiteWeapon`、任務獎勵、快捷鍵設定），名稱只用於顯示。
   共用倉庫的**金幣不是物品**，存在獨立的 `warehouseGold` 表，不混進倉庫物品表。
   唯一例外是 `getRequiredScrollItemId()` 依樓層區段組出卷軸名再換 id，
   換算只發生在該函式內，且由 `itemIdIntegrity.test.ts` 全段掃過

## 99.2 進行中的分階段計畫（完成後刪除）

### 妖魔森林／明鏡森林各拆成三段（使用者定案）

**動機**：兩區各自 Lv.30~40 跨 11 級卻只有 4 種怪、一組掉落、一張圖，等級涵蓋太廣。

**定案（使用者已確認，不得自行更動）**：

| 陣營 | 區域 | id | 等級 | 主題 |
|---|---|---|---|---|
| 艾爾薩斯 | 妖魔森林 | `demon-forest`（保留） | 30~33 | 侵蝕森林外緣 |
| | 腐葉林道 | `rotleaf-path`（新） | 34~36 | 腐生密林的獸徑 |
| | 妖魔祭壇 | `demon-altar`（新） | 37~40 | 巨石環繞的獻祭遺址 |
| 瓦爾登 | 明鏡森林 | `mirror-forest`（保留） | 30~33 | 反光疏林 |
| | 幻光湖畔 | `glimmer-shore`（新） | 34~36 | 開闊鏡湖群 |
| | 碎鏡深林 | `shattered-mirror`（新） | 37~40 | 碎裂鏡岩密林 |

- **每區 5 種怪**（共 30 種）：既有 8 隻依等級落回三段，新增 22 隻
- **不加場地王**，BOSS 任務池不變
- **新素材 4 種進 T4 配方**：4 個新區加入 `assignCraftMaterials.mts` 的 T4 區帶
  （7 區 → 11 區）後重跑 `--write`，既有 T4 配方會被重新洗牌
- **順帶降低 `魔法書碎片`（id 127）的配方佔比**：它掉在雪原（T4 區帶）與象牙塔 1~5F（T5 區帶），
  而那兩個素材池都只有「3 種真素材 + 碎片」，池小取得多 → 目前 27 把在吃它（12 T4／15 T5）。
  **不排除，改設使用上限：每階最多 3 把**（使用者定案），排序上放到池尾、額滿即跳過。
  **代價是 T5 配方也會一起重洗**
- 原 id 保留 → 存檔座標、既有掉落表、已定案地圖不動

**新地圖個性指派**（同主題「密度＋主導地形」必須兩兩相異，由測試強制）：

| 地圖 | 原型 | 密度 | 主導地形 | 目標可通行率 |
|---|---|---|---|---|
| `rotleaf-path` | 半開闊 | 密集 | 樹木 `10` | 70% |
| `demon-altar` | 半開闊 | 極密 | 岩石 `11` | 62% |
| `glimmer-shore` | 半開闊 | 標準 | 水池 `13` | 78% |
| `shattered-mirror` | 半開闊 | 密集 | 岩石 `11` | 70% |

（既有 forest 三張：`wind-woods` 標準／草叢、`demon-forest` 極密／樹木、`mirror-forest` 密集／水池）

**id 起點**：怪物 200+、道具 151+、掉落 1016+

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1 | `09-dungeon.md` § 9.5／9.6／9.9／9.11 六區定義；`38-map-control.md` § 38.10 地圖數 53→57、§ 38.11.2 指派表 4 列 | ✅ 完成 |
| 2 | `mapData.ts` 4 個新 Region＋ZONES；`mapDesignRules.ts` 4 筆 profile | ✅ 完成 |
| 3 | `monsterSeeds.ts` 既有 8 隻重分配 + 22 隻新怪（依 § 28.1 Lv.31~40 曲線） | ✅ 完成 |
| 4 | `itemSeeds.ts` 4 種新素材；`dropSeeds.ts` 4 區掉落（金幣／素材／藥水／印記／T4 裝備） | ✅ 完成 |
| 5 | 手繪 4 張 30×20 forest 地圖 → `inspectMap.mts` 修到六項安全檢查全綠 | ✅ 完成 |
| 6 | `adventurerQuest.ts` A 級區域池＋城鎮池＋`QUEST_AREA_MAPPING` | ✅ 完成 |
| 7 | `assignCraftMaterials.mts`：T4 區帶加 4 區、`魔法書碎片`（id 127）每階上限 3 把，重跑 `--write` | ✅ 完成 |
| 8 | 同步文件：`25`／`27`（含 § 27.8 印記區）／`28`／`30`／`36`／`06-equipment-acquire`／`01-worldview`／`QuestsPage.tsx` | ✅ 完成 |
| 9 | 測試：`sigilDrops.test.ts` 區域清單、新區一致性 case、`npx tsc -b`＋全套 vitest | ✅ 完成 |

### 介面風格調整：粗線條 + 提高彩度（與角色 sprite 對齊）

**動機**：角色 sprite 是近黑粗描邊 + 高飽和色塊，介面是 1px 藍灰細線 + 低飽和 dark HUD，
兩者對比策略不一致，角色看起來像貼在別的遊戲上。

| 階段 | 內容 | 狀態 |
|---|---|---|
| 1 | 建 `client/demo/ui-style.html` + `uiStyleDemo.ts` 調校頁：現況 vs 候選並排，可即時調線寬／描邊色／飽和度／面板亮度／圓角／分區色相，含地圖與角色同畫面對照 | ✅ 完成 |
| 2 | 使用者在調校頁定案參數 | ✅ 定案（見下方「定案參數」） |
| 3 | `App.css` 抽 token：`--border-width-*`、`--outline-dark`，替換現有 136 處硬編碼 border | 未開始 |
| 4 | `pixi/mapThemes.ts` 地圖色同步（只改 CSS 會讓介面卡通化、地圖仍是灰褐色） | 未開始 |
| 5 | 更新 `34-ui-guidelines.md`（色彩語意／線條規範），補樣式回歸測試 | 未開始 |

### 定案參數（階段 3 照這個改）

| 項目 | 值 |
|---|---|
| 線寬 | `--border-width: 1.75px` |
| 描邊色 | `--outline-dark: #4a4585` |
| 介面飽和度 | 既有色票 S × 1.30，**L 不動** |
| 圓角 | × 1.25 → sm 5px / md 10px / lg 15px |
| 地圖飽和度 | `mapThemes.ts` S × 1.60 |
| 區色濃度 | `--area-tint: 0.35`，**只上容器** |
| 文字對比 | `--text-dim: #64748B → #8A99B5`（3.4:1 → 5.9:1） |

**數量徽章**（定案）：
- 底色 `--badge-bg: #F59E0B` + 字色 `--badge-fg: #0A0A1A`（9.1:1）。
  現況白字在 `--accent-primary` 上只有 **2.7:1**，且紫正是 `.panel-dock-btn.active` 的背景色
- 字級改吃 `--fs-xs` —— `.quest-count-badge` 現在寫死 `10px`，不跟 `--font-scale`（違反 § 34.6）
- 兩顆徽章統一成角落圓形（`.script-badge` 現在是 inline 膠囊，兩套視覺講同一件事）
- **自動腳本不再顯示條數**，改成「有規則才亮」的小點。條數回答不了玩家會問的問題
  （「現在有沒有在跑」），而且 `ScriptEditorPanel.tsx` 的
  `totalRules = combatRules.length + persistentRules.length` **漏算村莊規則**
  （面板有三分頁，`selectVillageRules` 存在卻沒被算）—— 只寫村莊腳本的玩家會看到 0。
  小點只表達有／無，三個分頁怎麼加都不會算錯

**面板按鈕分組**（定案：**排法 2**）：
現況沒有分組語言（dock 內 6px、⚙ 前 8px，差 2px 看不出來）。三組照**功能**分，不是照「誰有徽章」：

| 組 | 按鈕 |
|---|---|
| 角色 | 詳細狀態 / 裝備欄 / 背包 / 技能 |
| 目標與自動化 | 任務 / 自動腳本 |
| 系統 | ⚙ |

組間 `--dock-group-gap: 16px`、組內維持 6px。
**最終排序**（`PanelDock.tsx` 要照這個改）：
`詳細狀態 → 裝備欄 → 背包 → 技能 ┊ 任務 → 自動腳本 ┊ ⚙`
—— 自動腳本留在最右不動，任務從最左移到它旁邊。

**1200px 以下是圖示模式且空間已緊**（`App.css` 註解記著 badge 曾被擠到溢出），
大間隔只在 >1200px 給，圖示模式維持緊排。

**村莊列（`.town-npc-bar`）不加容器框**（使用者定案）：它是一排浮在地圖上的按鈕，沒有面板底，
框起來等於憑空生出容器。村莊綠只活在 **active 的設施鈕**上 —— 那才是「你正開著哪一間」。
`.shop-items` 則要補容器框（它在清單面板內，本來就該有邊界）。

**限制**：
- 調校頁只吃 `src/App.css`，候選樣式寫在 demo 頁的 `<style>` 內，階段 2 定案前不進 `src/`
- 調色只做同一組色的 S 倍率 / L 偏移，**不動 H**、不引入新色票
- `--text-*` 不跟著調（低飽和灰藍拉飽和度會整片泛藍，且直接動到對比度）
- 線寬要決定吃不吃 `--ui-scale`（縮到 80% 時粗框會顯胖，見 `34-ui-guidelines.md` § 34.6）

**階段 1 的結論（供階段 2 選擇）**：
輪廓要成立，線與面必須有明度差。現在面板是近黑（L≈8%），**近黑描邊在上面等於看不見** ——
只加粗線寬不會變粗獷，只會變糊。兩條互斥路線：

| 路線 | 做法 | 代價 |
|---|---|---|
| A/B 亮邊 | 面板維持近黑，描邊改亮色 | 改動最小；輪廓感是「發光的線」，不是角色那種厚塗描邊 |
| C/D 提亮面板 | 描邊維持近黑，面板 L 拉高 | 與角色 sprite 同一套語言，一致性最好；面板一亮文字對比就掉，`--text-*` 要一併重估 |

**面板是主體（使用者確認）**，因此走 A/B（暗底亮框）；RimWorld 那種「介面退到看不見」
不適用 —— 它成立的前提是面板只是查資料的工具。

**分區色相**（「彩一點」的真正來源，已進調校頁）：
整體拉飽和度只會讓紫的更紫；分區色相才會被感覺到。五個區色**就是既有的 accent token**，不新增色票：

| 區 | 色 | 表面 |
|---|---|---|
| 角色 / 系統 | `--accent-primary` | `.status-panel`、`.panel-dock` |
| 導航 | `--accent-info` | `.map-selector` |
| 村莊 | `--accent-success` | `.town-npc-bar` |
| 戰鬥 | `--accent-danger` | `.combat-log-window` |
| 物品 / 商店 | `--accent-gold` | `.quick-slot-bar`、`.shop-items` |

- 區色**只上框、標題、active 態，不上內容文字** —— § 34.1 已把 gold＝金幣、
  danger＝危險、info＝裝備基礎屬性 綁成語意色，同一個色不可同時當「區域」與「語意」
- **只上容器、不上每顆按鈕**，否則村莊列變成 12 個綠框（那是裝飾，不是分區）
- `.town-npc-bar` 與 `.shop-items` 目前**沒有容器框**，分區方案需要補上，否則這兩區沒有落點

**對比預算（決定濃度用的原則）**：
「高對比比較好嗎」要分兩層答，方向相反 ——

- **文字對比：要高，沒有取捨。** `--text-dim`（#64748B）在面板上只有 3.4:1，低於 WCAG 的 4.5:1，
  而它用在 `.npc-label`、`.quick-slot-key` 這些真的要讀的字。這是**既有缺陷**（現況與候選都不過），
  提高它不花任何視覺預算。調校頁的「文字對比修正」把它改成 `#8A99B5`（5.9:1 / 7.0:1）
- **框線對比：不該高。** 對比靠稀缺才有用。放置遊戲一開好幾小時、畫面幾乎不變，
  **會變的只有事件**（超重、瀕死、掉寶、升級、CD 好了），預算該幾乎全給它們。
  框永遠都在，把預算花在框上等於把警報聲變成背景音 —— 尤其戰鬥區用 `--accent-danger`，
  那是超重與 HP 見底在用的色

因此建議落點是**線寬 1.75px、區色濃度 0.35**（調校頁預設 B），不是先前試的 2.5px / 0.85。
按 D 極端可以看到框亮到什麼程度時超重警示就分不出來了。

**方向相反的兩條**：角色像貼在別的遊戲上，要調高的是**地圖飽和度**（背景，不跟事件搶注意力），
不是介面的。地圖那條往上、UI 區色那條往下，不可綁在同一個「彩一點」底下。

**順帶發現的缺口**：`App.css` **沒有低血量警示**（無對應 class），HP 8% 與 HP 78% 除了長度外完全一樣。
對比預算正花在永遠不變的框上，真正該喊的地方是啞的。

階段 3 另需處理三處沒走 token 的硬編碼色（滑桿吃不到）：
`.hud-topleft .status-panel`、`.combat-log-title`、`.combat-log-overlay .combat-log` 的 background。

**未排入階段的既有問題**（`ui-ux-pro-max` 掃出來，與本次配色無關，另外排）：
- `PANEL_ICONS` 與 `TownView` 的 `FACILITIES` 用 emoji 當圖示，專案已有 SVG 圖示系統
  （`GameIcon` + `assets/icons/`）卻沒用在這兩處；`panelWindowStore.ts` 註解記錄的「圖示撞號兩次」即其症狀
- `App.css` 有 3 個 `:focus`（全在輸入框且接 `outline: none`）、**0 個 `:focus-visible`**，
  鍵盤焦點在加粗彩框之後更看不見
